{
  "buildCommand": "vite build",
  "outputDirectory": "dist",
  "functions": {
    "server.ts": {
      "includeFiles": "server/**"
    }
  },
  "rewrites": [
    { "source": "/api/:path*", "destination": "/server.ts" }
  ]
}
